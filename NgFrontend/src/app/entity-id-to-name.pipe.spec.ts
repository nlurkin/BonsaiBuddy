import { EntityIdToNamePipe } from './entity-id-to-name.pipe';

describe('EntityIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EntityIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
