import { PeriodIdToNamePipe } from './period-id-to-name.pipe';

describe('PeriodIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PeriodIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
